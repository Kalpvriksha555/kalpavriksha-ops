# Chatbox Restore Minimal Fix

Scope: Chat UI only.

What changed:
- Restored the chatbox from the last version where it was visible.
- Fixed sidebar/member-name clipping by widening the chat panel and sidebar.
- Kept the chat floating button behavior unchanged.
- Added grouped emoji board: Quick, Work, Mood.
- Improved attachment preview sizing so large images do not hide the sidebar.
- No backend, attendance, operations, admin, case, OTP, or other module files were intentionally changed.

Keep the existing .env unchanged.
