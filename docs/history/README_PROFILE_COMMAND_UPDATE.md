# Kalpvriksha Designs Ops - Command/Profile Stability Update

Changes in this build:

- Command Centre is now the first/default page before Operations Board.
- Command Centre includes daily operations, carry-forward pending work, team availability, payment health, urgent revisions, SLA tracking, and latest activity.
- Khushbu Pandey spelling is normalized from old saved/local data as well as the default team list.
- Login page name changed to `Kalpvriksha Designs Ops`.
- Forgot password/recovery option added on login page.
- Added `My Profile` page for every user with photo upload and optional important details:
  - Mobile number
  - Email
  - Designation
  - Aadhaar number
  - PAN number
  - Emergency contact
  - Address
  - Bank/UPI details
- Admin details remain optional. Designers and managers are reminded to fill details, but fields are not blocked/mandatory.
- Build checked with `npm run build`.

Run:

```powershell
npm install
npm run dev
```
