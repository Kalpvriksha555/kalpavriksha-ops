# Phase 13D — Mobile Chat Fix

## Fixed
- Message three-dot options now open reliably on mobile.
- Message menu no longer closes immediately due to message-list click bubbling.
- Direct-message user strip is hardened for horizontal scrolling on mobile.
- Mobile chat panel is forced to true full-screen so bottom/page layers do not interfere.
- Mobile channel strip supports touch pan-x and hidden scrollbar.

## Validation
- Frontend build passed.
- Backend syntax check passed.
